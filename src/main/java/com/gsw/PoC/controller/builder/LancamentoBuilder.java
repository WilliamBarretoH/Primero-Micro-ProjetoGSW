package com.gsw.PoC.controller.builder;

import org.springframework.stereotype.Service;

import com.gsw.PoC.controller.dto.LancamentoDTO;
import com.gsw.PoC.controller.dto.UsuarioDTO;
import com.gsw.PoC.model.entity.Lancamento;
import com.gsw.PoC.model.entity.Lancamento;

@Service
public class LancamentoBuilder {

private Lancamento lancamento;
	
//	public Lancamento builder(LancamentoDTO lancamentoDTO) {
//		return lancamento = Lancamento.builder().valor(lancamentoDTO.getValor())
//				.tipoLancamento(lancamentoDTO.getTipoLancamento())
//				.usuario(lancamentoDTO.getUsuario())
//				.build(); 
//	}
}